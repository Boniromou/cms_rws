$('input#search').click();
if(<%=@transaction.status == 'completed'%>){
var html = "<%= escape_javascript(render :partial => "shared/slip_form", :locals =>{:operation => @operation}, :formats => [ :html ]) %>"

printHtml(html);
}
