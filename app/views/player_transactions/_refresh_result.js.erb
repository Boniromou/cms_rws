$('input#search').click();
<% if @transaction && @transaction.status == 'completed' && policy(:PlayerTransaction).print_void? %>
  var html = "<%= escape_javascript(render :partial => "shared/void_slip_form", :locals =>{:operation => @operation}, :formats => [ :html ]) %>"
  printHtml(html, true);
<% end %>
