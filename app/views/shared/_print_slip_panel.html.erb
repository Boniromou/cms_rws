<br/>
<% if policy(:PlayerTransaction).print? %>
  <button id="print_slip" class="btn btn-primary">Print</button>
<% end %>
<%= link_to balance_path + "?member_id=#{@player.member_id}" , :title => t("button.close"), :remote => true, :id => "close_link", :class => "btn btn-primary" do %>
  <span> <%= t("button.close")%></span>
<% end %>
<%= link_to print_path + "?member_id=#{@player.member_id}" , :title => t("button.Print"), :remote => true, :id => "print_link" , :style => "display: none" do %>
  <span> <%= t("button.Print")%></span>
<% end %>

<%= javascript_include_tag "print.js" %>

<script type='text/javascript'>
  $('#print_slip').click(function() {
    $('#print_link').click();

    var html = $("#slip").html();

    printHtml(html, true);

    return false;
  });
</script>
